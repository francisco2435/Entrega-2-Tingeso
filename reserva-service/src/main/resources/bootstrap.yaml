spring:
  application:
    name: reserva-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/bdreserva
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: franciscoriquelmenunez@gmail.com
    password: gyll ndof lnus hrja
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true