server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/francisco2435/Entrega-2-Tingeso
          search-paths: config-data
  application:
    name: config-service
