spring:
  application:
    name: usuario-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/bdusuario
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update