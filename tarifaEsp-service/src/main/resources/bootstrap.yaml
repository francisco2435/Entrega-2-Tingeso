spring:
  application:
    name: tarifaEsp-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/bdtarifaEsp
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update