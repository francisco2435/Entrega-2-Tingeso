spring:
  application:
    name: descuentoClienFrec-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/bdDescuentoClientFrec
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update